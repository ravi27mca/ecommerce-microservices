spring:
  application:
    name: CONFIG-SERVICE

  cloud:
    config:
      server:
        git:
          uri: https://github.com/ravi27mca/spring-app-config
          clone-on-start: true

#eureka:
#  instance:
#    prefer-ip-address: true
#  client:
#    fetch-registry: true
#    register-with-eureka: true
#    service-url:
#      defaultzone: ${EUREKA_SERVER_ADDRESS:http://localhost:8761/eureka}
#

server:
  port: 9296





